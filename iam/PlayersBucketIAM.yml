PlayersBucketIAM:
  Effect: Allow
  Action:
    - s3:PutObject
    - s3:PutBucketPolicy
    - s3:GetBucketPolicy
    - s3:ListBucket
    - s3:GetObject
  Resource: arn:aws:s3:::${self:custom.PlayersBucket.name}/*
