AuctionsTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:GetItem
    - dynamodb:DeleteItem
    - dynamodb:Scan
    - dynamodb:Query
  Resource:
    - ${self:custom.auctionsTable.arn}
    - !Join ['/', [ '${self:custom.auctionsTable.arn}', 'index', 'statusAndEndDate' ]]