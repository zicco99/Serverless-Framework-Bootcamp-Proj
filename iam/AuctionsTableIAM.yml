AuctionsTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:GetItem
    - dynamodb:DeleteItem
    - dynamodb:Scan
    - dynamodb:Query
  Resource:
    - !GetAttr AuctionsTable.Arn
    - !Join ['/', [ !GetAttr AuctionsTable.Arn, 'index', 'statusAndEndDate' ]]